<div id="splash" class="float-container">
  <h1>Entrar</h1>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { id: 'sign-in-form', class: 'devise-form' }) do |f| %>
    <div class="inputs">
      <%= f.label :email, 'Correo electrónico' %>
      <%= f.email_field :email, :autofocus => true %>
      </div>

    <div class="inputs">
      <%= f.label :password, 'Contraseña' %>
      <%= f.password_field :password %>
    </div>

    <% if devise_mapping.rememberable? -%>
      <div class="inputs"><%= f.check_box :remember_me %> <%= f.label :remember_me, 'Recuérdame' %></div>
    <% end -%>

    <div class="inputs"><%= f.submit "Entrar", class: 'button belize-hole' %></div>
  <% end %>

  <%= render "devise/shared/links" %>
</div>